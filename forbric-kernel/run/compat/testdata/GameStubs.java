/** Compiled into the test's "game" jar: the declarations the game ships, without the Mixin-added field. */
public class GameStubs {
	public static class GameHolder { Object held; }
	public static class AddedHolder { }
}
